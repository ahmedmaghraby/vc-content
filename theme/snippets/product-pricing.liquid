


{% if product.price.actual_price.amount > 0 %}
    {{ product.price.actual_price | money }}
{% else %}
    {% if product.metafields.properties.Pricing.size > 0%}
        {{ product.metafields.properties.Pricing }}
    {% else %}
        {{ 'apps.general.free' | t }}
    {% endif %}
{% endif %}
{%comment%}
{{product.price.actual_price | json }}
{%endcomment%}
{% if product.price.actualPrice.amount > 0 %}
    {{ product.price | money }}
{% else %}
    {% if product.properties['Pricing'].value %}
        {{ product.properties['Pricing'].value }}
    {% else %}
        {{ 'apps.general.free' | t }}
    {% endif %}
{% endif %}

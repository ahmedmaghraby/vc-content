<div class="block {{block.backgroundColor}} {{block.topPadding}} {{block.bottomPadding}}
    {% if block.topDivider == 'True' %} block--divider-top {% endif %}
    {% if block.bottomDivider == 'True' %} block--divider-bottom {% endif %}">
    <div class="block__inner container">
        {% if block.titleStyle == 'h1' %}
            <h1 class="block__t">{{block.title}}</h1>
        {% elsif  block.titleStyle == 'h2' %}
            <h2 class="block__t">{{block.title}}</h2>
        {% elsif block.titleStyle == 'h3' %}
            <h3 class="block__t">{{block.title}}</h3>
        {% else %}
            <div class="block__t">{{block.title}}</div>
        {% endif %}

        {% if block.subtitle != '' %}
            <div class="block__space"></div>
            <div class="block__descr">{{block.subtitle}}</div>
            <div class="block__space block__space--size-lg"></div>
        {% else %}
            <div class="block__space block__space--size-lg"></div>
        {% endif %}

        {% if block.items.size > 0 %}
            <div class="list list-items list-items--cols-2">
                {% for item in block.items %}
                    {% if item.type == 'company' %}
                        <div class="list__item">
                            <div class="list__descr">{{item.review}}</div>
                            <div class="list__country">
                                <div class="list__country-img">
                                    {% if item.companyLogo != '' %}
                                        <img src="{{item.companyLogo.url}}" />
                                    {% endif %}
                                </div>
                                <div class="list__country-text">
                                    {{item.companyTitle}}
                                    {% if item.site.urlText != '' and item.site.url != '' %}
                                        <a href="{{item.site.url}}">{{item.site.urlText}}</a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    {% else %}
                        <div class="list__item">
                            <div class="list__descr">
                                <img src="{{item.image.url}}" class="list__quote" />
                                {{item.review}}
                            </div>
                            <div class="list__links">
                                <div class="list__links-item">{{item.name}}</div>
                                <div class="list__pos">{{item.jobTitle}}</div>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>